# yaml-language-server: $schema=https://raw.githubusercontent.com/compose-spec/compose-spec/a08f8d51631b7127ddac3d2c65537db429d0f1d0/schema/compose-spec.json

services:
    boilerplatemcforge-devcontainer:
        container_name: boilerplatemcforge-devcontainer
        image: ${IMAGE_REGISTRY:-ghcr.io/imesense}/mcforge1.12.2-modification-boilerplate-devcontainer:${IMAGE_TAG:-latest}
        platform: linux/amd64
        user: minecraft
        working_dir: /src
        volumes:
            - ${VOLUME_SOURCES:-..}:/src:delegated
            - ${VOLUME_GRADLE:-../.gradle}:/gradle:delegated
        networks:
            - boilerplatemcforge-devcontainer
        command:
            - "tail"
            - "-f"
            - "/dev/null"

networks:
    boilerplatemcforge-devcontainer:
        name: boilerplatemcforge-devcontainer
        driver: bridge
